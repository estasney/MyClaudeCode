# Python SQLAlchemy

- Use modern SQLAlchemy 2.x result methods (`.one_or_none()`, `.all()`, `.scalar()`) instead of deprecated 1.x methods (`.fetchone()`, `.fetchall()`). [python, sqlalchemy, api, deprecation, result-methods]
