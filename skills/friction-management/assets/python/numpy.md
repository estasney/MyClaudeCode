# Python NumPy

- Avoid for loops when working with numpy arrays. Use vectorized operations with numpy functions like np.isin, np.where, np.unique instead of iterating over arrays. Vectorization is more performant and idiomatic. [python, numpy, performance, vectorization]
